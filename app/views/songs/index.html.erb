<h1>All Songs</h1>
<% @songs.each do |song| %>
  <ul>
    <li><%= link_to song.title, song_path(song) %><br>
    Released: <%= song.release_year %><br>
    Artist: <%= song.artist_name %><br>
    Genre: <%= song.genre %></li>
  </ul>
<% end %>